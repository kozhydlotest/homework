// Spinner.jsx
import React from 'react';
import './Spinner.css'; // Assuming the CSS is in this file

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
